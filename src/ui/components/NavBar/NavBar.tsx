import { FC } from "react";

export const NavBar: FC = () => {
    return (
        <nav
            className={`
             flex flex-row
             items-center justify-start   
            `}
        >

        </nav>
    )
}